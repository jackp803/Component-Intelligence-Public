schema_version: '1.0'
phase:
  id: PHASE-11
  name: Verification
  execution_policy:
    gate_required_before_next_phase: true
tasks:
- id: T092
  version: 1
  title: SourceAuthority
  objective: 完成「SourceAuthority」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T091
    required_artifacts:
    - T091-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T091/T091-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: &id001
    - src/ComponentIntelligence/Verification/
    - tests/ComponentIntelligence.Tests/Verification/
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '建立固定來源優先順序。


      ---'
  outputs:
    artifacts:
    - id: T092-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「SourceAuthority」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: high
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T093
  version: 1
  title: FieldEvidence Model Integration
  objective: 完成「FieldEvidence Model Integration」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T092
    required_artifacts:
    - T092-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T092/T092-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '每一規格能附 Evidence。


      ---'
  outputs:
    artifacts:
    - id: T093-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「FieldEvidence Model Integration」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T094
  version: 1
  title: FieldComparator
  objective: 完成「FieldComparator」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T093
    required_artifacts:
    - T093-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T093/T093-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '比較 normalized same-key values。


      ---'
  outputs:
    artifacts:
    - id: T094-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「FieldComparator」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T095
  version: 1
  title: ConflictDetector
  objective: 完成「ConflictDetector」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T094
    required_artifacts:
    - T094-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T094/T094-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '不同可信來源不同值 → CONFLICT。


      ---'
  outputs:
    artifacts:
    - id: T095-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「ConflictDetector」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T096
  version: 1
  title: Verification Status Decision
  objective: 完成「Verification Status Decision」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T095
    required_artifacts:
    - T095-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T095/T095-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '產生：

      VERIFIED / SINGLE_SOURCE / CONFLICT / NOT_FOUND / ...


      ---'
  outputs:
    artifacts:
    - id: T096-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「Verification Status Decision」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T097
  version: 1
  title: CompletenessCalculator
  objective: 完成「CompletenessCalculator」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T096
    required_artifacts:
    - T096-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T096/T096-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '依 category profile 計算找到多少。


      ---'
  outputs:
    artifacts:
    - id: T097-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「CompletenessCalculator」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T098
  version: 1
  title: ConfidenceCalculator
  objective: 完成「ConfidenceCalculator」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T097
    required_artifacts:
    - T097-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T097/T097-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '依 source quality / conflict / evidence 計算可信等級。


      不得直接等同 Completeness。


      ---'
  outputs:
    artifacts:
    - id: T098-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「ConfidenceCalculator」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T099
  version: 1
  title: WiringReadiness
  objective: 完成「WiringReadiness」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T098
    required_artifacts:
    - T098-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T098/T098-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '第一版 Sensor 至少需：

      - power

      - connector or cable

      - relevant pins

      - output/protocol


      ---'
  outputs:
    artifacts:
    - id: T099-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「WiringReadiness」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T100
  version: 1
  title: TopologyReadiness
  objective: 完成「TopologyReadiness」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T099
    required_artifacts:
    - T099-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T099/T099-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '檢查：

      - ports

      - protocol

      - direction / connection constraints


      ---'
  outputs:
    artifacts:
    - id: T100-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「TopologyReadiness」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T101
  version: 1
  title: ValidationReadiness
  objective: 完成「ValidationReadiness」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T100
    required_artifacts:
    - T100-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T100/T100-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '檢查：

      工程驗證必要欄位是否足夠。


      ---'
  outputs:
    artifacts:
    - id: T101-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「ValidationReadiness」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T102
  version: 1
  title: DrawingReadiness
  objective: 完成「DrawingReadiness」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T101
    required_artifacts:
    - T101-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T101/T101-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: '檢查：

      繪圖必要資料；Image/CAD 缺失可依繪圖模式降級。


      ---'
  outputs:
    artifacts:
    - id: T102-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「DrawingReadiness」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
- id: T103
  version: 1
  title: VerificationEngine Integration
  objective: 完成「VerificationEngine Integration」所需的最小實作，且只涵蓋本 Task 規格。
  non_goals:
  - 不改寫原始來源值。
  - AI-only evidence 不得 VERIFIED。
  - 不得自行執行下一個 Task。
  dependencies:
  - task_id: T102
    required_artifacts:
    - T102-result
  inputs:
    references:
    - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
    - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
    artifacts:
    - artifact://T102/T102-result
  source_of_truth:
  - docs/spec/Component_Intelligence_System_Master_Spec_v0.1.md
  - docs/spec/Component_Intelligence_System_Worker_Task_Spec_v0.2.md
  execution_scope:
    allowed_roots: *id001
    protected:
    - .git/**
    - docs/spec/**
    - docs/source_of_truth/**
    max_production_files_changed: 4
    max_test_files_changed: 3
  implementation:
    spec_excerpt: 'PHASE 11 GATE：

      同值 → VERIFIED；

      衝突 → CONFLICT；

      完整度與可信度分開輸出。


      ---'
  outputs:
    artifacts:
    - id: T103-result
      type: task_result
      required: true
  acceptance:
  - id: AC-001
    type: deterministic
    statement: 「VerificationEngine Integration」可成功編譯並符合主規格。
  - id: AC-002
    type: deterministic
    statement: 本 Task 的 production change 必須限制在 execution_scope.allowed_roots，且不得修改 protected 路徑。
  validation:
    profiles:
    - dotnet_build
    - dotnet_test_all
    all_must_pass: true
  retry:
    max_total_task_attempts: 3
  side_effects:
    level: LOCAL_REVERSIBLE
  priority: normal
  stop_condition: Build 與本 Task 相關 deterministic tests 全部 PASS 後立即停止；不得自行開始下一 Task。
